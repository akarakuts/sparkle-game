# Copy to keystore.properties (gitignored) for local signed release builds.
# Same upload keystore as RuStore / GitHub Actions (see README).
storeFile=upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
